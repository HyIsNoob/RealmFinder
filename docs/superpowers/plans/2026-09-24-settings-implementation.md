# RealmFinder 1.1.0 settings implementation

1. Add gameplay rule defaults, validation, persistence, and client preference persistence. Cover rule decisions and migration with tests.
2. Apply server rules to capture costs, container contents, mob equipment, and carving. Test NBT filtering and rule behavior.
3. Add bounded settings request, sync, and update packets. Validate operator permission and send authoritative values.
4. Build an O-key settings screen with labels, hover tooltips, save/cancel/reset, and read-only server controls for non-operators. Add Undo confirmation and optional HUD hints.
5. Add English CurseForge description and short changelog, update README and Vietnamese smoke test, set version 1.1.0.
6. Run focused tests, full clean build, inspect JAR metadata, and report manual gameplay checks still needed.
